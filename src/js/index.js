let name = 'Richard Parker';

/**
 *  Realiza a soma de dois números
 * @param {Number} x 
 * @param {Number} y 
 * @return {Number}
 */
function soma(x, y) {
    return x + y;
}

class Animal {
    constructor(nome) {
        this.nome = nome;
    }
}